/**
 * Renders the dashboard landing page
 */
export default function DashboardPage() {
  return (
    <main className="relative flex min-h-screen flex-col gap-6 px-5 py-10">
      <p className="text-sm font-medium uppercase tracking-[0.2em] text-n-950">
        Pixsheet
      </p>
      <h1 className="text-3xl font-semibold text-n-950">Dashboard</h1>
    </main>
  );
}
